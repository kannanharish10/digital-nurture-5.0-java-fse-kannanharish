# Exercise 13 - Colors and Backgrounds

Demonstrates standard contextual styling classes and gradient layers.

Files:
- 13-1-dashboard.html — a simple analytical card panel using contextual color containers (`bg-primary`, `bg-success`, `bg-warning`, and `bg-danger`) with contrast text
- 13-2-gradient.html — premium display block styled using a combination of `.bg-dark` and the gradient shader helper `.bg-gradient`
