Bootstrap Icons assets would live here if the icon pack were extracted locally.

For this workspace, the icon examples use the installed `bootstrap-icons` package
from `node_modules` inside the exercises that need it.
